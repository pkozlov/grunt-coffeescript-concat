'use strict';

var path = require('path');

module.exports = function (grunt) {

  var _ = grunt.util._;

  grunt.registerMultiTask('coffeescript-concat', '', function () {
    var options = this.options({
      
    });

  });
};